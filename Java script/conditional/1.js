// Write a JavaScript program that displays the largest integer among two integers.

let n1 = parseInt(prompt("Enter the first number:"));
let n2 = parseInt(prompt("Enter the secound number:"));


if(n1 > n2)
{
    window.alert("the largest integer Int is" + n1);
}
else
{
    window.alert("the largest integer Int is" + n2);
}